/*
 *  AUTO-GENERATED stub.
 *
 *  The inline-SVG payload (~700KB of base-64-ish XML) that lived here is no
 *  longer needed: `MulberrySymbol` resolves every glyph through the static
 *  `MULBERRY_ASSET_MAP` in `mulberryAssetMap.generated.ts`. Removing the
 *  payload drops the same weight from the JS bundle.
 *
 *  This file is kept only so old imports still type-check. To render a
 *  Mulberry glyph:
 *
 *      import { MulberrySymbol } from '@/components/symbols/MulberrySymbol';
 *      <MulberrySymbol symbolId="mulberry_happy_xxxxxx" size={56} />
 *
 *  Delete this file once no consumers reference `MULBERRY_SVGS` or
 *  `MulberryKey`.
 */

export const MULBERRY_SVGS: Record<string, string> = {};
export type MulberryKey = string;
