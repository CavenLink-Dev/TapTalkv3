Rounded checkbox with an animated check — consent rows and completable task / list items.

```jsx
<Checkbox checked={agreed} onChange={setAgreed} label="I agree to the privacy policy" />
```

Checked state fills green with a white tick. 28px box for an easy tap target.
