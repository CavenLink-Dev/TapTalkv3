iOS-style on/off toggle for settings (parental lock, sound, activity enable/disable).

```jsx
<Switch checked={lockOn} onChange={setLockOn} />
```

On = primary fill, off = soft-blue track. Thumb springs across. Pass `disabled` to dim.
